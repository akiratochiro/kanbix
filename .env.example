# API (apps/api). Em produção, estas vão no painel do Render — veja DEPLOY.md.

DATABASE_URL="postgresql://kanbix:kanbix_dev_password@localhost:5434/kanbix?schema=public"

JWT_SECRET="coloque-um-segredo-forte-aqui"

# Origem(ns) do front permitida(s) pelo CORS (separadas por vírgula).
# Sem esta variável (dev), a API aceita qualquer origem.
# CORS_ORIGIN="https://kanbix.vercel.app"
